
---

# Лабораторная работа: Принципы проектирования SOLID

**Цель работы:** Научиться выявлять нарушения принципов SOLID и проводить рефакторинг кода для создания гибких систем.

---

## 1. Single Responsibility Principle (S) — Принцип единственной ответственности

> **Суть:** Каждый класс должен отвечать только за одну задачу.

### ❌ Антипаттерн (Плохой код)

Класс `InvoiceManager` считает сумму, печатает чек и сохраняет данные в базу.

```java
class InvoiceManager {
    public void process(double price, int count) {
        double total = price * count; // Расчет
        System.out.println("Чек на сумму: " + total); // Вывод
        // Логика сохранения в БД...
    }
}

```

**🛠 Задание:** Разделите логику на три независимых класса: `InvoiceCalculator`, `InvoicePrinter` и `InvoiceRepository`.

---

## 2. Open/Closed Principle (O) — Принцип открытости/закрытости

> **Суть:** Код должен быть открыт для расширения, но закрыт для изменения.

### ❌ Антипаттерн (Плохой код)

Чтобы добавить новый тип уведомления, приходится переписывать метод `send`.

```java
class NotificationService {
    public void send(String message, String type) {
        if (type.equals("Email")) {
            /* отправить Email */
        } else if (type.equals("SMS")) {
            /* отправить SMS */
        }
    }
}

```

**🛠 Задание:** Создайте интерфейс `NotificationSender`. Реализуйте классы `EmailSender` и `SmsSender`. Сделайте так, чтобы `NotificationService` мог работать с любым новым типом отправителя без изменения своего кода.

---

## 3. Liskov Substitution Principle (L) — Принцип подстановки Барбары Лисков

> **Суть:** Наследники не должны ломать логику базового класса.

### ❌ Антипаттерн (Плохой код)

Класс `ElectricCar` наследует `Car`, но метод `fillGasoline` (заправить бензином) для него бессмыслен и выдает ошибку.

```java
class Car {
    public void fillGasoline() { /* заправка */ }
}

class ElectricCar extends Car {
    @Override
    public void fillGasoline() {
        throw new RuntimeException("Я заряжаюсь от розетки!");
    }
}

```

**🛠 Задание:** Измените иерархию. Выделите общие черты в базовый класс, а методы заправки/зарядки вынесите в специфичные подклассы или интерфейсы.

---

## 4. Interface Segregation Principle (I) — Принцип разделения интерфейса

> **Суть:** Клиенты не должны зависеть от методов, которые они не используют.

### ❌ Антипаттерн (Плохой код)

Интерфейс «Многофункциональное устройство» заставляет обычный принтер уметь сканировать и отправлять факс.

```java
interface MultiFunctionDevice {
    void print();
    void scan();
    void fax();
}

class SimplePrinter implements MultiFunctionDevice {
    public void print() { /* печатает */ }
    public void scan() { /* не умеет, но метод реализовать обязан! */ }
    public void fax() { /* не умеет */ }
}

```

**🛠 Задание:** Разделите один «толстый» интерфейс на три маленьких: `Printer`, `Scanner`, `Fax`.

---

## 5. Dependency Inversion Principle (D) — Принцип инверсии зависимостей

> **Суть:** Зависьте от абстракций (интерфейсов), а не от конкретных реализаций.

### ❌ Антипаттерн (Плохой код)

Класс `WeatherTracker` жестко привязан к конкретному классу `EmailLog`. Мы не можем заменить его на отправку в Telegram без переписки кода трекера.

```java
class WeatherTracker {
    private EmailLog logger = new EmailLog(); // Жесткая связь

    public void notify(String status) {
        logger.send(status);
    }
}

```

**🛠 Задание:** Создайте интерфейс `Notifier`. Сделайте так, чтобы `WeatherTracker` принимал этот интерфейс через конструктор.

---

### 💡 Контрольные вопросы для самопроверки:

1. К какому принципу относится правило: "Не делай из класса швейцарский нож"?
2. Почему использование `instanceof` в больших цепочках `if-else` обычно нарушает принцип Open/Closed?
3. В чем разница между наследованием и композицией в контексте SOLID?
