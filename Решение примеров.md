

---

## 🛠 Примеры решений задач SOLID

### 1. Решение Single Responsibility (S)

**Что сделали:** Разделили обязанности. Теперь расчет не зависит от того, как мы печатаем данные или куда их сохраняем.

```java
// 1. Только расчет
class InvoiceCalculator {
    public double calculateTotal(double price, int count) {
        return price * count;
    }
}

// 2. Только вывод
class InvoicePrinter {
    public void print(double total) {
        System.out.println("Чек на сумму: " + total);
    }
}

// 3. Только работа с данными
class InvoiceRepository {
    public void save(double total) {
        // Логика сохранения в БД
    }
}

```

### 2. Решение Open/Closed (O)

**Что сделали:** Использовали паттерн **«Стратегия»**. Теперь для добавления уведомления через Telegram нам не нужно трогать `NotificationService`, достаточно создать новый класс.

```java
interface NotificationSender {
    void send(String message);
}

class EmailSender implements NotificationSender {
    public void send(String message) { /* логика Email */ }
}

class SmsSender implements NotificationSender {
    public void send(String message) { /* логика SMS */ }
}

class NotificationService {
    public void sendNotification(NotificationSender sender, String message) {
        sender.send(message); // Сервис не знает, КТО отправляет, он просто вызывает метод
    }
}

```

### 3. Решение Liskov Substitution (L)

**Что сделали:** Убрали общее наследование там, где оно логически неверно. Не все машины заправляются бензином.

```java
abstract class Car {
    public abstract void move();
}

interface GasolineCar {
    void fillGasoline();
}

interface ElectricCar {
    void chargeBattery();
}

// Теперь мы не подставим электрокар туда, где ждут заправку бензином
class Tesla extends Car implements ElectricCar {
    public void move() { /* едет тихо */ }
    public void chargeBattery() { /* заряжается */ }
}

```

### 4. Решение Interface Segregation (I)

**Что сделали:** Разделили один «жирный» интерфейс. Теперь классы реализуют только те методы, которые им действительно нужны.

```java
interface Printer { void print(); }
interface Scanner { void scan(); }
interface Fax { void fax(); }

// Простой принтер теперь не обязан уметь сканировать
class SimplePrinter implements Printer {
    public void print() { System.out.println("Печать..."); }
}

// МФУ реализует все нужные интерфейсы
class MultiFunctionMachine implements Printer, Scanner {
    public void print() { /*...*/ }
    public void scan() { /*...*/ }
}

```

### 5. Решение Dependency Inversion (D)

**Что сделали:** Применили **Dependency Injection (Внедрение зависимостей)**. `WeatherTracker` теперь не создает логгер сам, а получает его извне через интерфейс.

```java
interface Notifier {
    void send(String message);
}

class EmailLog implements Notifier {
    public void send(String message) { /* отправка почты */ }
}

class TelegramLog implements Notifier {
    public void send(String message) { /* отправка в бот */ }
}

class WeatherTracker {
    private final Notifier notifier;

    // Внедряем зависимость через конструктор
    public WeatherTracker(Notifier notifier) {
        this.notifier = notifier;
    }

    public void notify(String status) {
        notifier.send(status);
    }
}

```

---

### Подсказка для преподавателя:

Обратите внимание студентов на решение 5-й задачи (`WeatherTracker`). Теперь мы можем легко протестировать этот класс, передав ему «поддельный» (Mock) логгер, который ничего никуда не отправляет, а просто фиксирует вызов. Это и есть главное практическое преимущество **Dependency Inversion**.
